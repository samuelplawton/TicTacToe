{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run app",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/buildtemplate_app",
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",
      "externalConsole": true,
      "preLaunchTask": "CMake: build"
    }
  ]
}
