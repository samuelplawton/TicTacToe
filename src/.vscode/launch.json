{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ${workspaceFolderBasename}.exe",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\build\\${workspaceFolderBasename}.exe",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build (DevLauncher)",
      "console": "integratedTerminal"
    }
  ]
}
